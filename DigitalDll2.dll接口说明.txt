数字激活控件, .net4.6库,带过程输出。两个函数.
Sub DigitalActivat()
Function GetResult(ByRef szRes As String) As Integer
新建线程


vb.net:
        Dim func As New DigitalDll.DigitalFunc
        Dim szRes As String = ""
        Dim OldResult As String = ""
        func.DigitalActivat()
        Dim flags As Integer = func.GetResult(szRes)
        Do
            If flags <> 0 Then
                Exit Do
            End If
            flags = func.GetResult(szRes)
            If szRes <> OldResult Then
                OldResult = szRes
                Console.WriteLine(szRes) '输出激活过程
            End If
            Application.DoEvents()
        Loop



